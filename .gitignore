node_modules/
out/
*.vsix
.DS_Store

# Local secrets — never commit (holds VSCE_PAT / OVSX_PAT etc.)
.env
.env.*

# Build output: media/player.js is bundled by esbuild from src/webview/main.ts
media/player.js

# Playwright MCP output (screenshots / preview harness used for UI verification)
.playwright-mcp/
